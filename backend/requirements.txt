# Match Sport 24 backend - production dependencies
# Trimmed from the original Emergent template: removed unused AI/LLM/cloud SDKs
# (openai, litellm, google-generativeai, boto3, tiktoken, huggingface_hub, ...) and
# dev-only tooling (black, flake8, mypy, pytest, isort, ...) that server.py never
# imports - they only slowed down installs and bloated the deploy image.
# All versions below are unchanged from the previously working, tested set.

# Web framework / ASGI
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0

# Database
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

# Auth / security
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.1.3
cryptography==46.0.5
ecdsa==0.19.1
rsa==4.9.1
pyasn1==0.6.2
pyasn1_modules==0.4.2

# Validation
pydantic==2.12.5
pydantic_core==2.41.5
email-validator==2.3.0
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# Real-time chat
python-socketio==5.16.1
python-engineio==4.13.1
bidict==0.23.1
websockets==16.0
wsproto==1.3.2
simple-websocket==1.1.0

# Payments
stripe==14.4.0

# HTTP client (used for outbound calls, e.g. push notifications)
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.12.1
sniffio==1.3.1
certifi==2026.2.25
idna==3.11

# Config / utils
python-dotenv==1.2.1
python-dateutil==2.9.0.post0
tzdata==2025.3
